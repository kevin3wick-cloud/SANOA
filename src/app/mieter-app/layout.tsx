import { ReactNode } from "react";

export const metadata = {
  title: "Sanoa · Mieter",
  description: "Anliegen melden und Tickets verfolgen"
};

export default function MieterAppLayout({ children }: { children: ReactNode }) {
  return children;
}
